export default class Product {
  // constructor dùng để chứa dữ liệu
  constructor(id, name, price, img, desc) {
    this.id = id;
    this.name = name;
    this.price = price;
    this.img = img;
    this.desc = desc;
  }
}
